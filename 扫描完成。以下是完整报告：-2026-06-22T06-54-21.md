扫描完成。以下是完整报告：

---

## 📋 ICU 监护系统硬编码颜色扫描报告

### 一、项目现状摘要

| 指标 | 数值 |
|------|------|
| .vue 文件总数 | 81 |
| 含 `<style>` 的 .vue 文件 | 73 |
| 含 `html[data-theme='light']` 覆盖的 .vue 文件 | ~3050 行（跨多个文件） |
| .vue 文件中 hex 硬编码总数 | **3,341** |
| .vue 文件中 rgba/rgb 硬编码总数 | **3,548** |
| .vue 文件中已用 CSS 变量 `var(--*)` | **仅 197** |
| CSS 文件（style.css + mobile.css + clinical-documents-dark.css）硬编码 | 562 |

**核心问题：** `style.css` 已经定义了 `:root`（深色默认）和 `html[data-theme='light']`（浅色覆盖）的完整变量集，但 73 个 .vue 组件几乎没有使用，全部硬编码了深色模式颜色值。部分组件虽然加了 `html[data-theme='light']` 覆盖块，但也是再次硬编码另一组颜色值，而非引用变量。

### 二、主题切换机制（已确认）

```html
<!-- App.vue L3 -->
<div class="root" :class="[isMobileRoute ? 'theme-light' : `theme-${themeMode}`]">

<!-- App.vue L240-242 -->
function applyTheme(mode: 'dark' | 'light') {
  document.documentElement.setAttribute('data-theme', mode)
}
```

- 切换方式：`html[data-theme="light"]` / `html[data-theme="dark"]`
- **不是** `html.dark` class（你消息里提到的 `html.dark` 与实际代码不符，请注意）
- 默认主题：深色（`themeMode` 默认 `'dark'`）

### 三、style.css 现有变量体系

style.css 已有约 80+ 个变量，结构如下：

| 选择器 | 作用 |
|--------|------|
| `:root`（L10–L124） | 深色模式默认值 |
| `html[data-theme='light']`（L125–L238） | 浅色模式覆盖 |

但这些变量在 .vue 组件中的采用率极低（197 vs 6,889 硬编码）。

### 四、高频硬编码颜色聚类分析

#### 4.1 深色背景色（→ 应映射到 `--bg-base` / `--bg-surface` / `--bg-surface-2`）

| 颜色值 | 出现次数 | 推测用途 | 建议变量 |
|--------|---------|---------|---------|
| `#0f172a` | 96 | 页面/深色面板底色 | `--bg-base` |
| `#16324f` | 70 | 卡片/面板背景 | `--bg-surface` |
| `#223a54` | 29 | 次级面板底色 | `--bg-surface-2` |
| `#1f3852` | 21 | 次级区域 | `--bg-surface-2` |
| `rgba(8,28,44,.78)` | 多次 | 半透明表面 | `--bg-surface` |
| `rgba(7,20,34,.86)` | 多次 | 半透明面板 | `--bg-surface` |

#### 4.2 白色/近白色（→ `--text-primary` / `--bg-surface` 浅色模式）

| 颜色值 | 出现次数 | 推测用途 | 建议变量 |
|--------|---------|---------|---------|
| `#ffffff` / `#fff` | 140 | 主文字（深色模式）/ 卡片背景（浅色模式） | ⚠️ **双向问题**：深色模式用作文字，浅色模式用作背景，需按上下文分别映射 |
| `#f8fafc` | 52 | 浅色背景/卡片底 | `--bg-surface`（浅色） |
| `#f8fbff` | 47 | 浅色次级背景 | `--bg-surface-2`（浅色） |
| `#f3f8fb` | 19 | 浅色面板 | `--bg-surface`（浅色） |
| `rgba(255,255,255,.98)` | 51+ | 浅色卡片背景渐变 | `--bg-surface`（浅色） |

#### 4.3 文字颜色（→ `--text-primary` / `--text-secondary` / `--text-muted`）

| 颜色值 | 出现次数 | 推测用途 | 建议变量 |
|--------|---------|---------|---------|
| `#0f172a` | 94 | 浅色模式主文字 | `--text-primary`（浅色） |
| `#effcff` / `#ecfeff` | 88+77 | 深色模式主文字 | `--text-primary`（深色） |
| `#64748b` | 80 | 浅色模式次要文字 | `--text-secondary`（浅色） |
| `#47627e` | 58 | 浅色模式次文字变体 | `--text-secondary`（浅色） |
| `#91adbd` | 48 | 深色模式弱化文字 | `--text-muted`（深色） |
| `#6f8399` | 36 | 浅色模式弱化文字 | `--text-muted`（浅色） |
| `#8cb7c9` | 32 | 深色模式辅助文字 | `--text-secondary`（深色） |
| `#334155` | 36 | 浅色模式正文 | `--text-primary`（浅色） |
| `#8aa4b8` | 27 | 深色模式辅助 | `--text-muted`（深色） |

#### 4.4 品牌/强调色（→ `--brand`）

| 颜色值 | 出现次数 | 推测用途 | 建议变量 |
|--------|---------|---------|---------|
| `#1d4ed8` | 82 | 浅色模式品牌蓝 | `--brand`（浅色） |
| `#2563eb` | 41 | 浅色模式品牌蓝变体 | `--brand`（浅色） |
| `#22d3ee` | 22 | 深色模式品牌青 | `--brand`（深色） |
| `#67e8f9` | 64 | 深色模式强调/链接 | `--accent` |
| `#38bdf8` | 37 | 图表/指标线色 | 需新增 `--chart-*` 变量 |

#### 4.5 警告色（→ `--warning`）⚠️ 医疗告警关键色

| 颜色值 | 出现次数 | 推测用途 | 建议变量 |
|--------|---------|---------|---------|
| `#fde68a` | 41 | 警告文字/高亮 | `--warning`（深色） |
| `#fbbf24` | 38 | 警告强调 | `--warning`（深色） |
| `#f59e0b` | 28 | 警告色 | `--warning` |
| `#b45309` | 29 | 浅色模式警告文字 | `--warning`（浅色） |

#### 4.6 危险色（→ `--danger`）🚨 ICU 关键告警色

| 颜色值 | 出现次数 | 推测用途 | 建议变量 |
|--------|---------|---------|---------|
| `#fb7185` | 27 | 深色模式危险文字 | `--danger`（深色） |
| `#dc2626` | 24 | 危险/紧急 | `--danger` |
| `#fda4af` | 23 | 深色模式危险辅助 | `--danger-soft`（深色） |
| `#ef4444` | 23 | 危险色 | `--danger` |
| `#be123c` | 22 | 深色模式危险强 | `--danger-strong` |
| `#fecaca` | 28 | 浅色模式危险背景 | `--danger-bg`（浅色） |

#### 4.7 成功色（→ `--success`）

| 颜色值 | 出现次数 | 推测用途 | 建议变量 |
|--------|---------|---------|---------|
| `#34d399` | 34 | 深色模式成功色 | `--success`（深色） |
| `#22c55e` | 22 | 成功色 | `--success` |
| `#047857` | 18 | 浅色模式成功文字 | `--success`（浅色） |

### 五、特殊问题——白底隐形重灾区 🚨

**272 处** `rgba(255,255,255,x)` 模式：

| 模式 | 数量 | 风险 | 说明 |
|------|------|------|------|
| `rgba(255,255,255,.04/.06/.08)` | ~80 | 🔴 **白底完全隐形** | 深色模式中的微妙边框/内阴影，在白底上对比度≈0 |
| `rgba(255,255,255,.98)` | 51 | ✅ 浅色背景可用 | 仅在浅色模式有意义 |
| `rgba(255,255,255,.12/.14/.16)` | ~40 | 🔴 白底几乎不可见 | 边框/分割线在白底上消失 |

**310 处** box-shadow 硬编码 + **726 处** 渐变：

| 问题类型 | 典型示例 | 风险 |
|----------|---------|------|
| 深色发光阴影 | `0 0 16px rgba(248,113,113,0.28)` | 白底上发光效果消失，变成模糊色块 |
| 深色面板渐变 | `linear-gradient(180deg, rgba(9,22,36,.94), rgba(6,15,27,.92))` | 浅色模式下呈现为灰色脏块 |
| 半透明 surface | `background: rgba(8,28,44,.78)` | 白底上为深灰色矩形，与周围不协调 |

### 六、按文件的硬编码量排名（Top 20）

| 排名 | 文件 | 硬编码数 | data-theme='light' 覆盖行数 | 风险等级 |
|------|------|---------|---------------------------|---------|
| 1 | `views/PatientDetail.vue` | 475 | 224 | 🔴 极高 |
| 2 | `views/MdtBoard.vue` | 445 | 419 | 🔴 极高 |
| 3 | `components/patient-detail/AlertsTab.vue` | 400 | 157 | 🔴 极高 |
| 4 | `components/overview/PatientOverviewCard.vue` | 339 | 153 | 🔴 极高 |
| 5 | `views/ClinicalWorkflow.vue` | 273 | 59 | 🔴 高 |
| 6 | `views/PatientOverview.vue` | 268 | 70 | 🔴 高 |
| 7 | `views/Analytics.vue` | 245 | 83 | 🟡 中高 |
| 8 | `components/patient-detail/AiTab.vue` | 241 | 134 | 🟡 中高 |
| 9 | `views/ResearchWorkbench.vue` | 182 | 112 | 🟡 中 |
| 10 | `components/bigscreen/BigScreenBedGrid.vue` | 168 | 78 | 🟡 中 |
| 11 | `views/RoundingSheetView.vue` | 166 | 91 | 🟡 中 |
| 12 | `components/bigscreen/BigScreenAlertFeed.vue` | 160 | 73 | 🟡 中 |
| 13 | `views/NutritionSupportDashboard.vue` | 156 | 84 | 🟡 中 |
| 14 | `views/AiConsult.vue` | 154 | — | 🟡 中 |
| 15 | `views/NurseHome.vue` | 127 | 66 | 🟡 中 |
| 16 | `views/BigScreen.vue` | 116 | — | 🟠 中低 |
| 17 | `components/patient-detail/DigitalTwinTab.vue` | 111 | — | 🟠 中低 |
| 18 | `views/RespiratoryTherapistDashboard.vue` | 104 | — | 🟠 中低 |
| 19 | `components/common/HumanBodyDiagram.vue` | 99 | — | 🟠 中低 |
| 20 | `views/BedSideScreen.vue` | 99 | — | 🟠 中低 |

### 七、ECharts / JS 内嵌颜色（⚠️ CSS 变量无法直接覆盖）

以下文件在 JS/TS 代码中通过对象字面量传颜色给 ECharts，`var(--*)` **不可用**：

| 文件 | 典型位置 | 硬编码颜色数 |
|------|---------|------------|
| `views/PatientDetail.vue` | L1576–L1615（图表 series color） | ~20 |
| `views/Analytics.vue` | L913–L938（tooltip/line color） | ~15 |
| `views/ClinicalWorkflow.vue` | 图表配置 | ~10 |
| `components/patient-detail/AiTab.vue` | 图表配置 | ~10 |
| `views/MdtBoard.vue` | 图表/进度条 | ~5 |

**这些需要额外方案**（JS 常量映射 或 getComputedStyle 读取 CSS 变量），无法纯靠 CSS 变量解决。

### 八、需要你确认的问题

| # | 问题 | 详情 | 建议 |
|---|------|------|------|
| 1 | **主题切换机制** | 实际用的是 `html[data-theme='light']` 而非你提到的 `html.dark` class。是沿用现有机制，还是改成 class？ | 建议沿用 `data-theme`，改动最小 |
| 2 | **`:root` 默认值** | 你要求 `:root` = 浅色，但现有 `:root` = 深色。是否要翻转？ | 翻转会破坏所有直接写在 style.css 中的 Ant Design 覆盖（约 400+ 行），建议保持现有 `:root`=深色，`data-theme=light` =浅色 |
| 3 | **`#ffffff` 双重身份** | 深色模式中用作主文字色（正确），浅色模式中用作卡片背景。同一 hex 映射到不同变量 | 需要按上下文分别替换 |
| 4 | **ECharts JS 内嵌色** | ~60 处图表颜色无法用 CSS 变量 | 方案 A：JS 常量文件 + 主题切换读取；方案 B：getComputedStyle；方案 C：暂不处理 |
| 5 | **渐变/box-shadow** | 726 处渐变 + 310 处 shadow | 是否也纳入变量体系？还是仅处理 color/background？ |
| 6 | **`--text-muted` 对比度** | 深色 `#7f93ab` on `#07111d` ≈ 4.7:1 ✅，浅色 `#64748B` on `#F5F7FA` ≈ 4.6:1 ✅ | 当前勉强达标，但弱视用户可能仍有困难，是否提高到 7:1？ |
| 7 | **BigScreen 大屏组件** | 大屏模式（`BigScreen*.vue`）是纯深色还是也要支持浅色？ | 如仅深色，这些文件可以暂不改 |

### 九、建议映射方案（语义变量 → 颜色值）

基于你的需求，建议在 `style.css` 建立以下变量体系：

```
:root {  /* 深色模式（默认） */
  --bg-base:      #07111d;
  --bg-surface:   #0d1a2b;
  --bg-surface-2: #102033;
  --border-color: rgba(125, 167, 214, 0.14);
  --text-primary: #edf4fb;
  --text-secondary: #8ba0ba;
  --text-muted:   #7f93ab;   /* on #07111d ≈ 4.7:1 */
  --brand:        #22d3ee;
  --success:      #34d399;
  --warning:      #fbbf24;
  --danger:       #fb7185;
}

html[data-theme='light'] {
  --bg-base:      #F5F7FA;
  --bg-surface:   #FFFFFF;
  --bg-surface-2: #F1F5F9;
  --border-color: #E2E8F0;
  --text-primary: #0F172A;
  --text-secondary: #475569;
  --text-muted:   #64748B;   /* on #F5F7FA ≈ 4.6:1 */
  --brand:        #2563EB;
  --success:      #16A34A;
  --warning:      #D97706;
  --danger:       #DC2626;
}
```

（这与 style.css 现有变量有大量重叠，可以 alias 现有变量减少破坏。）

---

**总结：** 共发现 **~6,900 处**硬编码颜色分布在 **73 个 .vue 文件**和 **3 个 .css 文件**中。其中约 272 处白色半透明在浅色模式下完全隐形，是最大的视觉问题。请确认上述 7 个问题后，我开始分批修改。
