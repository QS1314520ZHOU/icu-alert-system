有 `composables/` 目录，但这个视图没在用。够了，下面我直接把分析和拆分计划交给你。

---

## 一、现状摘要

`frontend/src/views/PatientOverview.vue`：**1832 行 / 59KB**，单文件塞下了 8 类职责：

| 段 | 大致行号 | 职责 |
|---|---|---|
| Template | 2 – 335 | 顶栏统计 / 科室 nav / 标签 chips / 预测条 / 筛选条 / 风险分区 lane / 床卡列表 / 空态 |
| State & 路由同步 | 185 – 540 | 17 个 ref、route→dept/alert 同步、4 个 query 同步函数 |
| 告警/筛选/严重度纯函数 | 525 – 660 | `pickV / calcLevel / hasVitalValue / mergeVitals / mergeAlertLevel / isRescueRiskAlert / applyAlert / severityPriority` |
| 数据加载/水合 | 648 – 880 | `load / hydrateBedcards / hydrateBundleStatuses / hydrateForecastPreview / hydrateOverviewSignals` |
| 分类与组合 | 880 – 1180 | `filteredPatients / severitySections / deptTabs / tagStats / forecastStatus / activeOverviewFilters` 等 computed |
| 事件/动作 | 1180 – 1340 | `toggleAlert / selectDept / goDetail / clearOverviewFilters / loadDebounced` 等 |
| 生命周期 | 1340 – 1430 | `onMounted / onUnmounted / 轮询 / 全局事件订阅` |
| CSS | 1430 – 1832 | 顶部/筛选/床卡/light 主题样式 400+ 行 |

**问题**：
- 模板 333 行、脚本 ~1500 行、样式 ~400 行，任何改动都跨大半个文件。
- 大量纯函数和可复用状态（严重度合并、过滤、批量水合）可以下沉到 `composables/`，但目前没有用。
- 已经有 `PatientOverviewCard` 和 `PriorityFocusPanel` 两个子组件，但顶栏、筛选条、风险分区、空态仍是内联巨型块。
- 样式塞在 view 里，主题变体（light）和布局写在一起。

---

## 二、拆分目标

让 `PatientOverview.vue` 回归"页面骨架 + 数据编排"的角色，**目标 ≤ 400 行**：

- 顶栏 / 筛选条 / 风险分区 抽成独立组件
- 严重度/告警/过滤/数据水合 抽成 composable
- 样式 拆出独立 SCSS / 单独 scoped style 模块

---

## 三、拆分计划（按依赖顺序）

### Phase 1 — 抽 composables（零 UI 风险，回报最高）

放在 `frontend/src/composables/` 下，每个职责一个文件：

1. **`useOverviewFilters.ts`**
   - 状态：`curDept / tagFilter / alertFilter / rescueOnly / workflowFilter / domainFilter / priorityFilter`
   - 输出：`filteredPatients` / `severitySections` / `deptTabs` / `tagStats` / `activeOverviewFilters` 等 computed
   - 动作：`toggleAlert / clearOverviewFilters / selectDept / syncOverviewQuery / syncDeptQuery`
   - 依赖：route / router / auth store

2. **`usePatientSeverity.ts`**（纯函数，**无响应式**也 OK）
   - `pickV / vc / hasVitalValue / mergeVitals / calcLevel / severityPriority / mergeAlertLevel / isRescueRiskAlert / applyAlert`
   - 可被多个组件复用，先用 `import { ... } from` 方式导出函数（不强制 useXxx 包装）

3. **`useOverviewLoader.ts`**
   - 封装 `load()` 主流程
   - 暴露 `patients / depts / loading / refreshing / bedcardDataMap / forecastStatus / trajectoryConfig`
   - 内部串起 `getDepartments + getPatients + getBatchVitals + hydrate*`

4. **`useOverviewHydration.ts`**
   - `hydrateBedcards / hydrateBundleStatuses / hydrateForecastPreview / hydrateOverviewSignals / chunkItems`
   - 单独接受 `items` + `params`，便于单测

5. **`useOverviewPolling.ts`**
   - 顶栏轮询、刷新、全局事件订阅、bundleRequestToken / signalRequestToken 维护、卸载清理

> 验证：每个 composable 单测（用 `test_patient_resolve.py` 那一套前端 vitest 已有即可），并保证 PatientOverview.vue 行为不变。

### Phase 2 — 拆组件（按视觉边界）

1. **`OverviewHeader.vue`**（顶栏 + 统计 + 同步指示）
   - 行 4–34 区域
   - props：`counts: { total, critical, warning, normal }`、`refreshing`
   - emits：`toggleAlert`

2. **`OverviewDeptNav.vue`**（科室 nav）
   - 行 36–44
   - props：`deptTabs`、`curDept`；emits：`select`

3. **`OverviewTagChips.vue`**（标签 chips + 风险标签）
   - 行 46–64

4. **`OverviewForecastStrip.vue`**（预测状态条）
   - 行 65–71
   - 已存在 `useVitalForecast` composable，可直接消费

5. **`OverviewQuickFilters.vue`**（快速筛选面板：V2 分类 + 优先级）
   - 行 89–118
   - 双向绑定 v-model 到父组件传入的 refs / computed

6. **`OverviewSeverityLanes.vue`**（风险分区 lane-stack）
   - 行 125–329
   - 内部继续用 `PatientOverviewCard`
   - props：`severitySections`、`filteredPatients`；emits：`openDetail`

7. **`OverviewEmpty.vue`**（空态）
   - 行 331–333

最终视图变成：

```vue
<template>
  <div class="overview">
    <OverviewHeader :counts="counts" :refreshing="refreshing" @toggle-alert="toggleAlert" />
    <OverviewDeptNav :tabs="deptTabs" v-model:dept="curDept" />
    <OverviewTagChips ... />
    <OverviewForecastStrip ... />
    <OverviewQuickFilters v-model:domain="domainFilter" v-model:priority="priorityFilter" ... />
    <OverviewSeverityLanes
      v-if="severitySections.length"
      :sections="severitySections"
      :patients="filteredPatients"
      @open="goDetail"
    />
    <OverviewEmpty v-else-if="!loading" />
  </div>
</template>
```

### Phase 3 — 样式拆分

- 把 light/dark 变体整段（1450–1720）抽出 `frontend/src/styles/patient-overview.theme.scss`
- layout 段（1720–1832）抽 `OverviewView.scss`，view 改为 `<style lang="scss" src="...">` 或 CSS Modules
- 床卡 / 顶栏 / 筛选等子样式随组件走

### Phase 4 — 收口验证

- `npm run build` / `npm run typecheck`
- 启动 dev 跑通：进入 /overview 路径、切换科室、切换告警/标签/V2 分类、打开床卡、跳转 detail
- 跑前端单测（若已配置）
- 性能对比：观察首屏 hydrate 时间和打包体积

---

## 四、风险与对策

| 风险 | 缓解 |
|---|---|
| 拆分期间回归过滤/排序逻辑 | Phase 1 先抽 composable 但不删 inline，shadow 对比一次再切 |
| 轮询/全局事件清理漏掉 | 单独 `useOverviewPolling` 用 onScopeDispose 统一收口 |
| 床卡 v-model 传参多，props 变臃肿 | 提供 `useOverviewFilters` 作为 provide/inject，主视图只把 store 注入 |
| 主题样式打散 | 集中在 `styles/patient-overview.*` 统一引入 |

---

## 五、建议落地顺序与预计工作量

1. Phase 1 composables — 1 轮（先做 `usePatientSeverity` 和 `useOverviewFilters`）
2. Phase 2 拆 4 个轻组件（Header / DeptNav / TagChips / ForecastStrip）— 1 轮
3. Phase 2 拆重组件（QuickFilters / SeverityLanes）— 1 轮
4. Phase 3 样式 — 0.5 轮
5. Phase 4 验证 — 0.5 轮

合计 3–4 轮增量提交，每轮保持 `PatientOverview.vue` 可运行。

---

**我的建议**：先从 **Phase 1.2 `usePatientSeverity`** 开始——纯函数、好测试、改动最小、能为后续组件拆分铺路。要我直接开干这一刀吗？还是你想先看某个具体段落的细拆方案？
